# لا شيء من هذه القيم مطلوب: الأسرار تُولَّد تلقائياً وتُحفظ في قاعدة البيانات.
# اضبطها فقط إن أردت تجاوز القيم المولَّدة.

DATABASE_URL="file:./dev.db"

# اختياري — تجاوز المفاتيح المولَّدة تلقائياً
# AUTH_SECRET=""
# CRON_SECRET=""
# VAPID_PUBLIC_KEY=""
# VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:admin@example.com"
# APP_URL="http://localhost:3000"
